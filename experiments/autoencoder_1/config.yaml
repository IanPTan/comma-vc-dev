# Default hyperparameters for training autoencoder
batch_size: 8
epochs: 100
dataset: "data/frames.h5"
learning_rate: 0.001
num_workers: 0
seed: 42
save_frequency: 10
val_split: 0.0
best_set: "train"
qat: true

# Model architecture hyperparameters
base_channels: 8
num_layers: 4
bottleneck_channels: 64
